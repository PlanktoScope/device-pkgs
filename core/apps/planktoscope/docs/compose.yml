services:
  server:
    image: ghcr.io/planktoscope/project-docs:sha-8db8fcf-minimal
    volumes:
      - server_data:/data
      - server_config:/config

networks:
  default:
    name: none
    external: true

volumes:
  server_data:
  server_config:
