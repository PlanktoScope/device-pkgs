services:
  server:
    image: ghcr.io/planktoscope/project-docs:sha-a05e0e3-minimal
    volumes:
      - server-data:/data
      - server-config:/config

networks:
  default:
    name: none
    external: true

volumes:
  server-data:
  server-config:
