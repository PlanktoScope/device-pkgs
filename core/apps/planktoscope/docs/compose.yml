services:
  server:
    image: ghcr.io/planktoscope/project-docs:sha-8a8f8b8-minimal
    volumes:
      - server-data:/data
      - server-config:/config

networks:
  default:
    name: none
    external: true

volumes:
  server-data:
  server-config:
