services:
  server:
    image: ghcr.io/planktoscope/project-docs:2023.9.0-minimal
    volumes:
      - server-data:/data
      - server-config:/config

networks:
  default:
    name: none
    external: true

volumes:
  server-data:
  server-config:
